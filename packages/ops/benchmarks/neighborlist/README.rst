Neighbor List Benchmarks
=========================

Performance benchmarks for neighbor list construction algorithms, including:

* Cell list algorithm (O(N) scaling)
* Naive algorithm (O(N²) scaling)
* Batch processing variants
* Scaling with system size and batch dimensions
