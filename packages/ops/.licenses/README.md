# NVIDIA ALCHEMI Toolkit-Ops Dependency Licenses

This folder contains a `detais.json` that reports licenses for all **required**
dependencies of `nvalchemiops`. The files can be automatically generated in
a Docker image by invoking the `pip-licenses` `Makefile` target in the
root folder.
