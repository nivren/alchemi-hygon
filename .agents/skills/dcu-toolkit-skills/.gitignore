# 第三方 vendor 工具（自带 .git / 编译产物，不进仓库）
dcu-env/scripts/vendor_dcuprofiletools/

# 测试产物（真机采集的日志/报告，含现场信息，不进仓库）
dcu-env/test_runs/

# 真实凭证与真实设备清单（物理隔离在 ~/.config，仓库内只保留模板）
**/credentials.json
data/devices.json

# Python 缓存
__pycache__/
*.pyc

# 系统/编辑器临时文件
.DS_Store
*.swp
