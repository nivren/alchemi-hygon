# 上游锁定与导入依据

2026-09-05，按用户指定的两个完整 SHA 锁定，见 UPSTREAM_LOCK.yaml。
指令来源为本次用户指令、根 AGENTS.md、PROJECT_HANDOFF.md 和 START_HERE.md；历史路线文档与外部探索仅供参考。
初始主仓库 master 无提交，仅有未跟踪的交接文档和 .gitignore，无 packages 产品代码。已有真实 Git 身份 Wang, Leping / wangleping@dcu2。

两个 external origin 均为 NVIDIA 官方 GitHub URL，工作树干净，HEAD 正好等于锁定 SHA，is-shallow-repository=false。两仓库 `git fsck --full --no-dangling` 均退出 0；完整保留本地所含的可达历史，不声称核验远端所有 refs。根仓库从本地 clone 获取对象，使用不 squash subtree 独立导入提交；不修改 external。

framework 0.2.0 要求 Python >=3.11,<3.14、torch>=2.8、ops>=0.4.1；ops 0.4.1 要求 Python >=3.11,<3.15，torch extras >=2.8，基础依赖 warp-lang>=1.13.0、numpy。Python 3.12 / 海光 Torch 2.9 满足声明版本交集。AST 审计的 97 条 framework→ops 显式 from-import 均可从指定 ops 的模块/符号静态解析。脚本及完整 API、导入、测试、示例、配置和许可证清单见 probes/audit_upstream.py 与 reports/upstream_inventory.json。

这是**源码导入兼容基线**，不是 DCU 运行验证：静态符号解析不证明调用参数、动态导出、数值或二阶梯度正确。原始 Warp/NVIDIA 依赖尚未适配，未安装两包到探针环境。framework uv 的 dependency-metadata 对 ops 0.4.1 与本次源码依赖一致，但注释中的 Torch >=2.11 不适用于这两个锁定 pyproject 的实际 >=2.8 约束。不运行原包的 CUDA extras / uv sync。

两个根 LICENSE 均为 Apache-2.0；完整导入还保留 .licenses、内嵌第三方来源、SPDX、测试、CI 和文档。许可证清单记录于 lock/inventory，不将根许可证推广为所有第三方文件的许可证。

依赖风险：Hooks 直接使用 physicsnemo.utils.profiling；分布式还含 vendored upstream 与 shard 包装；不能整体删除 PhysicsNeMo 能力。当前 HCU 项目环境不安装该 NVIDIA/Warp 绑定包，单进程 Torch/HCU 通过可选导入路径，域并行与 profiling 保留为显式能力。MACE extra 固定 mace-torch==0.3.15，与 UMA 的 e3nn 版本要求存在冲突；当前项目环境使用 `mace-torch 0.3.15/e3nn 0.4.4`，探索环境使用 `mace-torch 0.3.16/e3nn 0.4.4`，cuEquivariance/UMA/compile 单列 C。保留 upstream tests；未运行的测试均不算通过。

旧探索 /home/wangleping/codes/nvalchemi-toolkit 和 /home/wangleping/codes/hyalchemi-ops 只读查阅了文件清单及后者 PROGRESS.md 的邻居/PBC/LJ 记录。其性能与数值记录未重跑、不纳入当前验证。后续按算子提取思路与测试案例，逐项检查来源、许可证和当前 SHA 语义后才移植。

## 实际导入记录

首次导入发生在历史分支 `codex/g0-initialization`；初始审计提交 `bd4c612`；framework subtree 提交 `52ffa2d`；ops subtree 提交 `88aa209`。两次导入均不 squash，锁定提交是对应导入提交的第二父提交，全部可达历史保留。B0 集成指针为 `team/dev-baseline-v0.1`，已按用户授权 fast-forward 合入共享 `develop`。

普通 fetch 仅获取 external 本地 main，而 external HEAD 锁定在另一个提交，首次 subtree 因对象不存在而退出 1、未导入目录。随后显式 `git fetch upstream-framework 4dfe3723def34df3fadb245981081ccf8c94c257` 和 `git fetch upstream-ops 26dbceb61e30cca80e1a5805eebeb51d7dc68fd1`，再执行 subtree，均成功。不得将默认 main 当作锁定 SHA。

`probes/verify_import.py` 退出 0：两个 packages 子树 tree hash 与上游完全相同，两个 SHA 均是 HEAD 祖先，无嵌套 .git。证据 reports/import-verification.json。external 保持干净。嵌入的原 CI 文件仅作为来源保留，不会自动作为根 CI 生效。

## 本地补丁清单

本节登记导入到 `packages/framework` 和 `packages/ops` 后对上游文件的修改。
新增的 reference 模块、兼容性测试和探针属于产品侧新增文件，不冒充上游补丁；
它们仍需在对应特性报告中记录来源和限制。表中“工作树待提交”是生成该清单时的历史
快照描述，不代表当前工作树；当前提交状态以 `git log` 和 `git status` 为准。周期邻居
Tier 1 状态检查点已提交为 `9fb9d1f`。

| ID | 文件与位置 | 相对锁定上游的变化 | 动机与影响 | upstream candidate | 回归/证据 |
|---|---|---|---|---|---|
| LP-001 | `packages/framework/nvalchemi/dynamics/base.py`：`BaseDynamics._mutable_fields` | 上游 `("positions", "velocities", "cell")`；本地增加 `"forces"`, `"energy"`, `"stress"` | 混合 status Batch 中，已收敛体系会经历共享前向的临时 pre-update；恢复输出字段可以避免返回的力/能量对应临时坐标。该行为同时作用于 Warp 默认路径，需在后续上游同步和 ADR 0004 中重新审阅。 | **候选**：需要向上游说明 mixed-status 输出一致性语义；当前不改变默认后端。 | `test/compatibility/test_inactive_dynamics_outputs.py`；`test/dynamics/test_base.py` 85 项；工作树待提交 |
| LP-002 | `packages/framework/test/dynamics/test_ops.py`：`device`/`backend` fixtures 及 VV/FIRE/FIRE2 调用 | 上游没有 `NVALCHEMI_TEST_DEVICE`、`NVALCHEMI_TEST_BACKEND`；本地允许测试显式选择设备和 reference backend | 复用锁定上游行为测试作为 Torch reference/HCU 载体，同时保留环境变量未设置时的 `backend=None` Warp 默认语义。仅修改测试，不改变产品 API 默认值。 | **不候选**：测试载体约定，不应进入上游产品接口。 | `reports/g2-upstream-dynamics-reference.md`；CPU/HCU 各 `23 passed`；默认 Warp 反向验证 `13` 项在 Warp 边界失败；工作树待提交 |
| LP-003 | `nvalchemi/dynamics/__init__.py`、`dynamics/_ops/{_bridge,fire,velocity_verlet}.py`、`dynamics/base.py`、`dynamics/hooks/`、`dynamics/integrators/`、`dynamics/optimizers/` | 将 Warp/ops 导入延迟到明确的 Warp 路径；公共固定晶胞 dynamics 接受显式 reference backend；`DynamicsStage` 通过顶层无 Warp 模块复用 | 解除公共导入的 eager Warp 阻断，保持 `None`/`warp` 默认路径，提供 `torch_reference`/`auto` 的显式委派；变量晶胞和 NPT/NPH 仍按需保留 Warp。 | **候选**：导入隔离和可选 backend 边界可向上游提议，但 reference 实现不属于本次上游同步。 | `reports/g2-dynamics-public-boundary.md`、`reports/g2-mace-dynamics-reference.md`；主要实现提交 `6ef6446`，后续工作树修改待提交 |
| LP-004 | `nvalchemi/data/batch.py`、`data/level_storage.py`；新增 `data/storage_backend.py`、`data/warp_storage_backend.py` | storage 操作从模块级 Warp helper 改为 backend 协议，新增 Torch reference 和显式 Warp backend | 保留 Batch/LevelStorage 数据语义，在无 Warp 环境可导入并验证 uniform/segmented copy、defrag、扩容和多属性 mutation；不把 storage 协议误写成邻居 staging K 扩容管线。 | **候选**：backend 边界可回馈上游；Hygon Torch 实现不直接上游化。 | `reports/g1-project-reference-pytest.md`、`reports/g1-neighbor-capacity-reference.md`；提交范围 `917cbfa`–`3f89f6b` |
| LP-005 | `nvalchemi/neighbors.py`、`hooks/neighbor_list.py`、`models/base.py`、`models/lj.py`、`models/mace.py`、`hooks/stage_timing.py` | 增加显式 Torch reference 邻居/LJ/MACE 接线、PBC/skin/cache/capacity 受限路径和按需导入 | 建立 Batch→neighbor→energy/force→MACE 的可验证 reference 链；默认 Warp API 保持不变，half-list、switching、stress、cell-list 和生产性能仍显式限制。 | **部分候选**：后端参数/导入隔离可讨论；Hygon reference 受限语义需单独维护。 | `reports/g1-pbc-lj-nve-reference.md`、`reports/g1-skin-rebuild-batch-reference.md`、`reports/g1-mace-wrapper-batch.md`；提交范围 `43f2e3d`–`cfe8a39`、`78f9a3f`–`a1b6e59` |
| LP-006 | `nvalchemi/hooks/__init__.py`、`training/__init__.py`、`training/hooks/__init__.py` | PhysicsNeMo/profiling/DomainParallel 由顶层强制导入改为按需可选导入 | 避免 HCU 单进程 Torch/reference 路径被 NVIDIA 绑定依赖阻断；显式 profiling 和域并行名称仍保留，未宣称其 HCU 支持。 | **候选**：可选依赖边界可能适合上游；不删除高级能力。 | `test/hooks/test_optional_imports.py`、MACE wrapper 报告；提交 `4be155f` |
| LP-007 | `packages/ops/nvalchemiops/{__init__,backend,torch_backend,torch_reference}.py` 及 `dynamics/`、`interactions/`、`jax/`、`math/`、`neighbors/`、`torch/` 的 `__init__.py` | 新增可审计 backend dispatcher/reference；Warp 初始化移至显式 Warp-facing 边界 | 让 `nvalchemiops` 顶层可导入，避免 reference 路径级联 `initialize_warp()`；未知 Triton/HIP 请求明确失败。 | **候选**：dispatcher 结构可参考上游；Hygon 后端和受限 reference 不是上游默认实现。 | `reports/g1-torch-reference-backend.md`、ops reference tests；提交 `05bd0f0`、`109e453` |
| LP-008 | `packages/framework/nvalchemi/dynamics/sinks.py`：`HostMemory.write` | 上游写入使用 `data.to(self._device)`；本地在设备迁移后追加 `clone()` | DataSink 是轨迹快照所有者；当输入已经在 CPU 时，`Tensor.to("cpu")` 可能保留原张量，后续 Batch 的 in-place 更新会覆盖已写入帧。显式 clone 使标准字段和动态 graph/system 字段均满足 snapshot decoupling；只影响 HostMemory，不改变 GPUBuffer/ZarrData 或默认 dynamics backend。 | **候选**：可向上游提交“HostMemory snapshots must not alias live Batch”语义；需要评估 CPU 写入开销和现有 sink API。 | `test/compatibility/test_heterogeneous_trajectory_reference.py` CPU/HCU；`test/dynamics/test_sinks.py -k 'HostMemory or DrainMethod or DataSinkABC'` 17 passed；工作树待提交 |
| LP-009 | `packages/framework/test/dynamics/test_inflight.py`：`TestFusedStageInflight.test_send_path_realigns_state_and_last_converged` | 上游直接构造 `FIRE(...)` 未提供 backend；本地读取 `NVALCHEMI_TEST_BACKEND` 并传入，未设置时仍为 `None`/Warp 默认 | 让锁定上游的 inflight/FusedStage 状态回归可以在显式 Torch reference 下运行；只改变测试载体，不改变产品默认路径，也不把 NVT/Langevin 的 Warp 依赖伪装成 reference。 | **不候选**：沿用 LP-002 的本地测试环境约定，不应改变上游产品接口。 | `reports/g2-upstream-inflight-reference.md`；CPU `10 passed, 3 skipped`，HCU `13 passed`；工作树待提交 |
| LP-010 | `packages/framework/test/dynamics/test_state_management.py`：固定晶胞 NVE/FIRE/FIRE2 构造器 | 上游固定晶胞状态测试未传 backend；本地读取 `NVALCHEMI_TEST_BACKEND` 并传入，未设置时仍为 `None`/Warp 默认；Langevin/Nose-Hoover/NPT 测试保持原样 | 复用上游状态初始化、state shape、state invariant、partial removal 和 `_make_new_state` 测试验证已完成的 Torch reference；限制改动只作用于固定晶胞组件，避免把未移植热浴/压强积分器伪装成 reference。 | **不候选**：与 LP-002/LP-009 相同，是本地测试载体约定。 | `reports/g2-upstream-fusedstage-state-sinks-reference.md`；显式 reference 固定晶胞子集 CPU `20 passed`；工作树待提交 |
| LP-011 | `packages/framework/nvalchemi/dynamics/hooks/_utils.py`、`hooks/logging.py`、`hooks/monitors.py`；`packages/framework/test/dynamics/test_observer_hooks.py` | 新增 observer 计算 `compute_backend`，reference 使用 Torch segment reduce/kinetics；上游 observer 测试增加 `NVALCHEMI_TEST_BACKEND` 载体 | 解除 Logging/energy-drift 在显式 reference dynamics 中触发 `_segmented_max` 或 kinetic Warp 的隐式阻断；`None`/`warp` 仍保留上游 Warp，`auto`/`torch_reference` 只显式选择 Torch reference。日志输出 backend（csv/tensorboard/custom）与计算 backend 分离。 | **部分候选**：可选 backend 边界和无 Warp observer 辅助函数可向上游提议；Hygon Torch 具体实现与默认选择需单独评审。 | `reports/g2-upstream-observer-reference.md`、`test/compatibility/test_public_dynamics_reference.py`；CPU observer `44 passed, 33 skipped` + compatibility `8 passed`、HCU observer `77 passed` + compatibility `8 passed`；默认反向单测在 Warp 边界失败；工作树待提交 |
| LP-012 | `packages/framework/nvalchemi/hooks/periodic.py`、`packages/framework/test/dynamics/test_hook_utils.py`、`packages/framework/test/dynamics/test_periodic_hook.py` | 将周期包裹 helper 的 Warp/ops 导入延迟到 Warp 分支；新增 `backend`/`compute_backend` 边界及 `_dynamics_reference.periodic` Torch custom op；上游工具/周期 hook 测试增加 `NVALCHEMI_TEST_BACKEND` 载体 | 解除 `test_hook_utils.py` 在模块收集阶段的隐式 Warp 阻断，并为 reference dynamics 的周期坐标维护提供与上游相同的原地语义；`None`/`warp` 保留默认 Warp，`auto`/`torch_reference` 仅显式走 Torch reference。WrapPeriodicHook 可从 workflow backend 继承计算后端。 | **部分候选**：延迟可选导入与 backend 边界可向上游提议；Hygon reference 实现和 Torch custom op 需单独评审。 | `packages/framework/test/dynamics/test_hook_utils.py`、`test_periodic_hook.py`；`probes/hooks_utils_reference.py`；CPU utility `30 passed, 5 skipped`、periodic hook `15 passed, 11 skipped`；项目环境 DTK 26.04、BW200/gfx936 HCU 的 probe 退出码 `0`，上游 utility+periodic reference 回归 `61 passed`（含 CUDA compile smoke）；受限沙箱中的 `No HIP GPUs are available` 仅表示 `/dev/kfd` 不可见；默认反向单测在 `_segmented_max` Warp 边界失败；工作树待提交 |
| LP-013 | `packages/ops/nvalchemiops/torch_reference.py`：`_periodic_neighbor_pairs` 与 `_neighbor_list_periodic` | 相对锁定上游的 periodic reference 实现改为 flat device tensors；使用 `nonzero`/`bincount`/行内 rank/scatter 替代逐边 Python `tolist()`/`append`/写回。几何候选、shift、自相互作用、padding、overflow 与 MATRIX/COO 顺序保持不变。 | 消除 HCU periodic full-list 装配中的 per-edge host 往返，保留 `torch_reference` 正确性优先和逐 system 几何路径；不改变上游 Warp 默认路径，也不宣称 cell-list/no-PBC/half-list 已完成。 | **部分候选**：device-side 装配结构可向上游反馈；Hygon reference 代码和性能取舍需单独评审。 | `reports/g2-neighbor-tier1-scatter-reference.md`；ops reference CPU/HCU 各 `10 passed`，framework compute_neighbors CPU/HCU 各 `3 passed`，真实 perf_46/92/184/368、等长 batch 和异构 Batch scatter 后回归；工作树待提交 |
| LP-014 | `packages/ops/nvalchemiops/{backend,torch_backend}.py`；framework `neighbors`、dynamics/hook/LJ backend 接线 | 将单一 reference resolver 升级为 operation/device/dtype/gradient/features capability registry；framework 删除局部字符串分支并委派统一选择，`auto` 记录一次选择；`make_neighbor_hooks` 增加 `backend` 并保留别名。 | `None`/`warp` 仍仅保留 legacy Warp 路径；当前 registry 只登记已验证 Torch reference，Triton/HIP 明确失败。LoggingHook 的 `backend` 是 writer，故保留 `compute_backend`。 | **候选**：集中 dispatcher 与可选依赖边界可讨论；当前 capability 宽度和 Hygon policy 为本地实现。 | `packages/ops/test/torch/test_backend_registry.py`、`reports/g2-backend-registry-no-pbc-reference.md`；项目 `.venv` 的 BW200 HCU 0 回归 `17 passed`。 |
| LP-015 | `packages/ops/nvalchemiops/torch_reference.py`：no-PBC `neighbor_list` 装配 | 将逐原子/逐边 Python 候选与矩阵写回替换为逐 system tensor pair geometry、`nonzero`、`bincount`、row-rank/scatter；保持 MATRIX/COO row-major 顺序、batch 边界、full/half、距离/向量、overlap 与 overflow 契约。 | 消除 no-PBC 每边 Python 写回，但仍是 O(N²) reference；不实现 cell-list、target rows、method/scratch、PBC half-list 或生产性能路径。 | **部分候选**：device-side 装配思路可讨论；本地 reference 范围需独立维护。 | `packages/ops/test/torch/test_torch_reference_backend.py`、`probes/no_pbc_neighbor_reference.py`、`reports/g2-backend-registry-no-pbc-reference.md`；CPU 与项目 `.venv` 的 BW200 HCU 0 通过，HCU ops 回归 `17 passed`。 |
| LP-016 | `packages/ops/nvalchemiops/backend.py`、`torch_backend.py`；framework `neighbors.py`、`hooks/neighbor_list.py`；新增 `torch_reference_cell_list.py` 与契约测试 | **历史过渡名称**：注册显式 `torch_reference_cell_list` capability，并接入 no-PBC uniform cell-list；后续由 LP-017 迁移为 `backend="torch_reference", method="cell_list"`。 | 为 cell-list 建立可审查的 opt-in 语义切片，候选仅来自 27 个邻近 cell，保留 full/half、MATRIX/COO、距离/向量、批边界、顺序和容量错误契约；periodic/target/scratch/rebuild 仍显式不支持。 | **不候选默认上游变更**：后端名称、Hygon reference 实现和 capability 选择需本地维护；契约和测试可作为后续讨论材料。 | `adr/0010-torch-reference-cell-list.md`、`reports/g2-torch-reference-cell-list-contract.md`；ops CPU/HCU `10 passed`，framework CPU/HCU `3 passed, 2 deselected`；真实规模性能尚未验证。 |
| LP-017 | `packages/ops/nvalchemiops/{backend,torch_backend}.py`；framework `_backend.py`、`neighbors.py`、`hooks/neighbor_list.py` | 将固定 backend literal/capability tuple 收敛为 `ImplementationRegistry`，登记 implementation ID/family/strategy/lazy executor；cell-list 迁移为 `backend="torch_reference", method="cell_list"`，dispatcher 按 implementation ID 执行。 | `None`/`warp` 保持 legacy 语义；`auto` 在 M2 Profile 前不选择 strategy；未发布的 `torch_reference_cell_list` 请求显式失败。只完成 neighbor 的单次 selection 传递，不实现 fingerprint/profile/planner。 | **不候选默认上游变更**：Hygon registry 语义和 Torch strategy 需本地维护；可审计的 implementation metadata 结构可作为后续讨论材料。 | ADR 0006、`reports/g2-backend-registry-m1.md`；CPU ops `25 passed`、framework `22 passed`；BW200 HCU ops `25 passed`、M1 framework strategy `2 passed`。 |
| LP-018 | framework fixed-cell dynamics/models/hooks/distributed backend call sites；ops `backend.py`/dispatcher | M1 follow-up 将 FIRE/FIRE2 拆为独立 operation/implementation ID，并让 LJ、VV、FIRE/FIRE2、kinetics、periodic、segmented reduction 和 observer 由 framework 解析一次 `BackendSelection` 后传给 dispatcher。 | 保证一次解析、精确 implementation ID 执行和 variable-cell capability 明确失败；不改变 `backend=None`/Warp 默认语义。 | **不候选默认上游变更**：selection propagation 和 reference backend 是本地平台适配边界。 | `reports/g2-backend-registry-m1-dispatch-propagation.md`；CPU selection/dispatch 与 reference slices 已通过；本轮未新增 HCU 证据。 |
| LP-019 | `packages/ops/nvalchemiops/backend.py`、`_backend_types.py`、`_backend_catalog/` | 将默认 implementation inventory 按 operation family 拆为 metadata-only catalog，保留 public registry API、稳定 ID、注册顺序和 lazy executor。 | 为多个开发者提供低冲突的 backend 登记边界；catalog 不复制实现、不改变当前能力宽度。 | **不候选默认上游变更**：Hygon registry/catalog 组织方式属于本地协作基础设施。 | `reports/team-dev-baseline-v0.1.md`；CPU gate 和 B0 HCU gate 均通过，未产生性能结论。 |
| LP-020 | `packages/ops/nvalchemiops/{_backend_types,backend,executor,dispatch,torch_backend}.py`；framework operation dispatchers；`_dynamics_reference/segmented_reduce.py` | 兑现 `Implementation.executor`：metadata 增加 entrypoints/owner，legacy 固定无 executor；新增 operation-neutral lazy loader/adapter；VV、FIRE/FIRE2、LJ、neighbors、periodic、kinetics/segmented 与 observer 改为声明式绑定。 | 消除 catalog 与 22 处 implementation-ID 分支的人工双写；legacy handler 以局部闭包保留，adapter 不含 operation 分派表；错误标注 executor 包归属。公共 API、默认 Warp 和 capability 宽度不变。 | **不候选默认上游变更**：这是 Hygon 多后端协作基础设施；entrypoint ABI 约束与 generic adapter 可作为后续上游架构讨论材料。 | `adr/0007-declarative-executor-binding.md`、`reports/b1-executor-binding.md`；CPU gate 通过；B1 golden-path HCU smoke 在 HCU 0 通过。 |
| LP-021 | `packages/ops/nvalchemiops/torch_reference_cell_list.py`、neighbor catalog；framework neighbor/Hook tests | 将本地 no-PBC Torch cell-list 扩展为 periodic/no-PBC、full/half、mixed/triclinic Batch；增加 build/query 分层、预分配 scratch、selective rebuild、image shift 和连续 vector/distance 梯度。 | 建立上游 cell-list core 的 Warp-independent correctness oracle，保持 `backend=None`/Warp 和 `auto` 不变；`target_indices`、pair callbacks/outputs、pair-centric/compile 与生产 HIP/Triton 仍显式延期。 | **部分候选**：契约测试、保守 triclinic 搜索半径和分层 API 可讨论；Hygon backend 选择与 Torch/HIP 性能实现需本地维护。 | `reports/g2-torch-reference-pbc-cell-list-core.md`；CPU gate 通过；BW200 HCU PBC probe、真实结构 benchmark/短 MACE-FIRE2 及标准 smoke 通过；真实基线显示 Torch cell-list 未达到性能门槛。 |
| LP-022 | `packages/ops/nvalchemiops/_cell_list_abi.py`；`torch_reference_cell_list.py` | 新增 shared cell-key build ABI，以 geometry/PBC 输入和预分配 `int32` shift/coordinate/key 输出 buffer 固定 native HIP/Triton 可替换边界；现有 Torch cell-list 改由该 reference 生成 key。 | 仅冻结 build/binning 子模块的私有 ABI，不登记 custom-op/backend，不改变 `backend=None`/Warp、`auto`、CSR/query 或能力宽度。 | **不候选默认上游变更**：Hygon 多后端 ABI 与原生后端接入策略需本地维护；PBC cell-key 契约测试可作为讨论材料。 | `reports/g2-cell-key-build-abi.md`；CPU focused `15 passed, 17 deselected`。本轮项目 PyTorch 无可见 HCU，未新增 HCU/perf 证据。 |
| LP-023 | `packages/ops/nvalchemiops/_hip_cell_key.py`；`_native/cell_key_build.cpp/.cu`；ops Hatch sdist source include | 在 shared ABI 之上增加 lazy native HIP mutation-only `torch.library.custom_op`、fake 注册、gfx936 loader 和当前 stream cell-key kernel；源码随 wheel 提供，loader 将 HIP 转换副本写入 cache。 | 仅建立可替换的 native HIP 装载边界，不登记 `hip` capability，不改变 `backend=None`/Warp、`auto`、CSR/query 或 fallback 语义；当前不是预编译 AOT wheel。 | **不候选默认上游变更**：Hygon native custom-op/构建边界需本地维护；kernel output parity 通过后再评估是否拆正式 AOT 或 query 模块。 | `reports/g2-cell-key-hip-boundary.md`；CPU boundary/reference `17 passed`，gfx936 compile/link/load、wheel source packaging 及主机 BW200 FP32/FP64 mixed-PBC/triclinic output parity、空输入、strided input、stream probe 通过；未测性能。 |
| LP-024 | `scripts/build_hip_cell_key_aot_wheel.py`；ops Makefile；`_hip_cell_key.py` AOT loader | 以 Hatchling 基础 wheel 加上现有 HIP artifact，生成带 CPython/platform tag、非 purelib 标记、RECORD/provenance 的 gfx936 AOT wheel；loader 优先加载 package 内 artifact。 | 只服务 cell-key 可选 native 模块的本机分发，artifact 加载错误显式失败；不替换上游打包后端，不登记 `hip`，不改 `backend=None`/Warp、`auto`、CSR/query 或 fallback 语义。 | **不候选默认上游变更**：Hygon DTK/Torch/gfx936 artifact 构建与 wheel 注入策略需本地维护。 | `reports/g2-cell-key-aot-wheel.md`；AOT wheel WHEEL/RECORD 校验通过，临时解包后 BW200 实际加载内置 `.so` 并通过 FP32/FP64 parity、空输入、strided input、stream probe；未测性能。 |
| LP-025 | `packages/ops/nvalchemiops/_cell_list_abi.py`；`torch_reference_cell_list.py` | 将 cell-key 后的 counts 与 exclusive starts 抽为 shared CSR phase，定义 int32 output、global offset、空输入、key-range 和 overflow 契约；Torch cell-list build 改由该 phase 写 active slices。 | 不实现 native atomic count/scan/sort/fill/query，不改 `backend=None`/Warp、`auto`、AOT artifact 或 `hip` capability。 | **部分候选**：cell-key CSR 契约与分层 API 可讨论；Hygon native module 计划需本地维护。 | `reports/g2-cell-key-csr-abi.md`；CPU focused `20 passed`，BW200 CSR/PBC cell-list probe 通过；未测性能。 |
| LP-026 | `packages/ops/nvalchemiops/_hip_cell_count.py`；`_native/cell_key_count.cpp/.cu`；`_cell_list_abi.py` | 在 shared count ABI 上新增 lazy native HIP mutation-only custom-op：current-stream output 清零后以 atomic add 统计 key；count-only Torch oracle 作为逐元素对照。 | 不接入 Torch reference、AOT wheel、dispatcher、`auto` 或 `hip` capability；CPU/非 HIP 明确失败，无静默 fallback；scan/sort/fill/query 不变。 | **不候选默认上游变更**：Hygon HIP custom-op/构建与性能选择需要本地维护；上游 Warp 的 `count_atoms` 同语义可作契约参考。 | `reports/g2-cell-key-hip-count-boundary.md`；CPU focused `25 passed`，BW200 JIT 编译/加载与普通、空、strided key、stream parity 通过；未测性能。 |
| LP-027 | `probes/cell_key_count_benchmark.py` | 新增固定 input、交替顺序、HIP-event 的 count-only Torch/HIP microbenchmark，并保留一值一行的 artifacts 原始样本。 | 仅测显式 count API 的 validation/zero/count device 时间；不计 JIT、scan/sort/fill/query、完整 build 或 host 时间，不改任何 runtime 选择。 | **不候选默认上游变更**：Hygon device/DTK 上的 HIP count 性能证据和后端选择需本地维护。 | `reports/g2-cell-key-count-benchmark.md`；BW200 32768 keys/4096 cells uniform/single-cell 的 Torch/HIP median factor `1.300x/1.295x`；仅局部 microbenchmark。 |
| LP-028 | `adr/0009-hip-cell-list-build-pipeline.md` | 对照上游 `count_atoms -> array_scan -> zero -> bin_atoms` 记录 HIP build pipeline、融合边界、cursor 生命周期、内部 order 与公开 neighbor order 的契约。 | 这是本地 DCU 后端设计记录；不实现 scan/fill/query、不改 public API、默认路径、`auto` 或 capability。 | **不候选默认上游变更**：HIP/rocPRIM 选择与 DCU 性能策略需本地维护；上游 CSR build 分阶段语义可作为讨论材料。 | DTK 26.04 headers/hipcc 只读发现；尚无 rocPRIM compile 或 HCU execution 证据。 |
| LP-029 | `packages/ops/nvalchemiops/{_cell_list_abi,_hip_cell_scan}.py`；`_native/cell_start_scan.cpp/.cu` | 新增 standalone CSR exclusive-start Torch oracle 与 lazy native HIP rocPRIM custom-op；workspace query、caller-owned scratch、global offset 和 mutation 语义显式化。 | 不接入 key/count/fill/query、AOT、dispatcher、`auto` 或 `hip` capability；CPU/非 HIP 明确失败，无静默 fallback。 | **不候选默认上游变更**：Hygon HIP/rocPRIM 装载与 workspace 策略需本地维护；上游 scan 阶段语义可作契约参考。 | `reports/g2-cell-start-scan-hip-boundary.md`；CPU focused `29 passed`，BW200 JIT compile/load 与 output parity、empty、strided counts、stream probe 通过；未测性能。 |
| LP-030 | `packages/ops/nvalchemiops/{_cell_list_abi,_hip_batch_cell_key_count}.py`；`_native/batch_cell_key_count.cpp/.cu` | 新增 Batch-aware fused geometry/PBC/key/count Torch oracle 与 lazy HIP custom-op；global cell offsets 固定异构系统的 concatenated CSR count ABI。 | 不接入 scan/fill/query、AOT、dispatcher、`auto` 或 `hip` capability；CPU/非 HIP 明确失败，无 silent fallback。 | **不候选默认上游变更**：Hygon HIP fusion 与后端选择策略需本地维护；上游 batch count-atoms 的语义可作契约参考。 | `reports/g2-batch-cell-key-count-hip-boundary.md`；CPU focused `33 passed`，BW200 JIT compile/load 与异构 Batch FP32/FP64、empty B=1、strided input、stream parity 通过；未测性能。 |
| LP-031 | `probes/batch_cell_key_count_fusion_benchmark.py` | 新增相同 global output contract 的 native Batch fusion 与 per-system native key/count/offset-add composition HIP-event benchmark，并保存一值一行 raw samples。 | 仅测 pre-warmed build-half GPU work，不计 JIT/Python/scan/fill/query/complete neighbor，不改任何 runtime selection。 | **不候选默认上游变更**：Hygon device/DTK 的 fusion performance 与后端选择需本地维护。 | `reports/g2-batch-cell-key-count-fusion-benchmark.md`；BW200 FP32 2x16384/4096-cell uniform/single-cell composed/fused factor `1.481x/1.483x`；仅局部 microbenchmark。 |
| LP-032 | `packages/ops/nvalchemiops/_cell_list_abi.py`；`torch_reference_cell_list.py` | 新增 CSR atom-list fill Torch oracle：显式 cursor scratch、stable key order、global atom offset、capacity tail 和 public counts/starts 只读合同；Torch reference build 复用该 ABI。 | 对照上游 `bin_atoms` 固定 fill 生命周期，同时为 native HIP fill 提供可验证替换边界；不实现 native fill、性能、dispatcher、`auto` 或 `hip` capability。 | **部分候选**：CSR fill 数据合同和测试可作为上游讨论材料；Hygon stable-order/reference 与 HIP cursor 策略需本地维护。 | `reports/g2-cell-atom-list-fill-abi.md`；CPU `27 passed`，BW200/gfx936 PBC reference HCU probe 通过；未有 native HIP fill 证据。 |
| LP-033 | `packages/ops/nvalchemiops/_hip_cell_fill.py`；`_native/cell_atom_list_fill.cpp/.cu` | 新增显式 native HIP `bin_atoms`-style atomic fill candidate：独立 cursor reset、global atom ID 与 current-stream insertion；同 cell atom order 明确未指定。 | 不接入 dispatcher、AOT、`auto` 或 `hip` capability；仅以 per-cell atom set 对照 stable oracle，未验证 public query order 或性能。 | **不候选默认上游变更**：Hygon HIP custom-op/loading 与 atomic-order 后续 canonicalization 需本地维护；上游 cursor 生命周期可作契约参考。 | `reports/g2-cell-atom-list-fill-hip-boundary.md`；CPU focused `37 passed`，BW200/gfx936 JIT compile/load/run 的 FP32/FP64、异构 Batch/PBC、strided、empty、stream cell-set parity 通过；未测性能。 |
| LP-034 | `packages/ops/nvalchemiops/_hip_batch_cell_build.py`；`probes/native_batch_cell_build_hip_boundary.py` | 以显式 Python composition 串接 fused key/count、rocPRIM scan 与 atomic fill，固定 Batch CSR build 的 caller-owned output/workspace 和 global atom offset 合同。 | 不新建 native kernel、dispatcher、AOT、`auto` 或 `hip` capability；不验证 query/public pair order，atomic list 仅以 per-cell set 对照。 | **不候选默认上游变更**：Hygon multi-extension composition、workspace 生命周期与后续 canonicalization 需本地维护；上游分阶段 build 语义可作参考。 | `reports/g2-batch-cell-csr-build-hip-boundary.md`；CPU focused `39 passed`，BW200/gfx936 三 extension 实际 JIT/load/run，FP32/FP64、异构 Batch/PBC、offset、strided、empty、stream CSR parity 通过；未测性能。 |
| LP-035 | `torch_reference_cell_list.py` query ordering tests；`probes/native_batch_cell_build_query_hip_boundary.py` | 验证现有 Torch direct query 的 final pair sort 将 per-cell atom-list permutation canonicalize，HIP atomic CSR build 可在此 query surface 保持 public output order。 | 不实现 native query，不增加 pair-centric/target/pair-output/compile 支持，不改 dispatcher、AOT、`auto` 或 `hip` capability。 | **部分候选**：atom-list order-independent direct-query regression 可作为上游讨论材料；Hygon HIP build composition/probe 需本地维护。 | `reports/g2-batch-cell-build-query-order-boundary.md`；CPU focused `43 passed`，BW200/gfx936 mixed PBC/no-PBC Batch FP32/FP64 full/half/stream public output parity 通过；未测性能。 |
| LP-036 | `probes/native_batch_cell_build_query_benchmark.py` | 对固定 Batch CSR build 与现有 Torch direct query 进行 build/query/full HIP-event 对照和受限 hipprof 探查，确定 query/materialization 是下一候选方向。 | 仅提供窄性能证据，不改变 native kernel、Torch query、dispatcher、AOT、`auto` 或 `hip` capability；profile 的 JIT/module load 与稳态 timing 分开解释。 | **不候选默认上游变更**：Hygon native HIP/Torch query benchmark 与 HIP/Triton 后端选择需本地维护；上游 query 语义可作 reference。 | `reports/g2-native-batch-cell-build-query-benchmark.md`；BW200/gfx936 FP32 2x2048 uniform/clustered build native 慢约 `18.4%`，full native 慢 `2.9%--3.4%`，query 基本持平；未测更大规模、FP64、端到端或 runtime。 |
| LP-037 | `probes/native_batch_cell_build_query_benchmark.py`；`reports/g2-batch-scale-performance-matrix.md` | 补充中小体系高 Batch 的完整 build/query/full HIP-event 矩阵：`46/92 atoms × 32/64 systems`，uniform/clustered，显式记录 global cells 与 capacity overflow 边界。 | 仅扩展性能证据维度，不改变 native kernel、Torch query、dispatcher、AOT、`auto` 或 `hip` capability；为 query/materialization candidate 的优先级提供代表性输入依据。 | **不候选默认上游变更**：Hygon device/DTK 性能矩阵与 HIP/Triton 后端选择需本地维护；上游 query 语义仍只作 reference。 | `reports/g2-batch-scale-performance-matrix.md`；BW200/gfx936 FP32 8 workloads 中 native build 慢 `31%--45%`，query 差异约 `-0.62%--+0.06%`，full 通常慢 `0.34%--0.63%`；未测 FP64 性能、rebuild、梯度、端到端或 runtime。 |
| LP-038 | `packages/ops/nvalchemiops/_hip_batch_cell_query.py`；`_native/batch_cell_query.cpp/.cu`；`probes/native_batch_cell_query_candidate_hip_boundary.py` | 新增 native HIP Batch query enumeration candidate：按 source atom 遍历邻近 cell，执行 PBC image/shift、cutoff、self 排除和 full/half 过滤；输出 unordered row candidates，Torch reference 负责 public materialization。 | 固化上游 direct atom-centric query 的最小可替换逻辑边界；不复制 stable public sort、distance/vector/autograd，不接 dispatcher、`auto`、`hip` capability，也不声明完整 upstream query 支持。 | **不候选默认上游变更**：Hygon HIP query kernel、加载和后端选择需本地维护；上游 `_query_pairs` 的候选枚举语义可作为 reference。 | `reports/g2-batch-cell-query-candidate-hip-boundary.md`；CPU boundary `2 passed`，BW200/gfx936 HCU FP32/FP64 mixed-PBC full/half/stream pair/shift/distance/vector parity 与显式 capacity overflow 通过；未测性能。 |
| LP-039 | `probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-materialization-benchmark.md` | 对高 Batch 小体系矩阵拆分 native enumeration、native enumeration + Torch public matrix/count/shift materialization 与完整 Torch query 的预热 HIP-event device-time。 | 仅提供 candidate 优先级和 query 瓶颈证据，不改变 native query kernel、public distance/vector/autograd、dispatcher、AOT、`auto` 或 `hip` capability；HIP event 不代表 API wall-clock。 | **不候选默认上游变更**：Hygon benchmark 与 HIP/Triton 后端选择需本地维护；上游 query 逻辑继续作为 reference。 | `reports/g2-batch-query-materialization-benchmark.md`；BW200/gfx936 FP32 8 workloads candidate factor `45.44x--101.16x`，matrix/count/shift parity 通过；未测 optional outputs、梯度、FP64 性能、端到端或 runtime。 |
| LP-040 | `packages/ops/nvalchemiops/_torch_batch_query_materialization.py`；`probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-geometry-materialization.md` | 将 native query candidate 的离散拓扑与 Torch public geometry materialization 分层：复用 reference stable ordering/scatter，并保留 positions/cells 的 distance/vector autograd；以 `--geometry` 复测高 Batch workload。 | 固化 candidate-to-public 的私有桥接合同，不改变上游 query 语义、dispatcher、AOT、`auto` 或 `hip` capability；benchmark 使用 HIP-event device timeline，不代表 API wall-clock。 | **不候选默认上游变更**：Hygon 后端子阶段划分和 HIP/Triton 选择需本地维护；上游 `_sort_pairs` 与 vector/distance 公式继续作为 reference。 | `reports/g2-batch-query-geometry-materialization.md`；CPU `6 passed`，HCU 0 FP32/FP64 geometry/public/gradient boundary 通过；FP32 8 workload geometry candidate factor `35.20x--79.49x`；未测 FP64 性能、API wall-clock、rebuild、target/pair outputs、compile 或端到端。 |
| LP-041 | `packages/ops/nvalchemiops/_torch_batch_query_materialization.py`；`probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-materialization-breakdown.md` | 对固定 native candidate 分别测 Torch stable topology canonicalization/scatter、geometry distance/vector 和完整 materialization helper，确定下一 HIP/Triton 优化热点。 | 只提供阶段级 device-time evidence，不改变上游 query 语义、public ordering、autograd、dispatcher、AOT、`auto` 或 `hip` capability；阶段计时不是 API wall-clock。 | **不候选默认上游变更**：Hygon topology optimization 与 HIP/Triton 后端选择需本地维护；上游 stable pair ordering 继续作为 oracle。 | `reports/g2-batch-query-materialization-breakdown.md`；HCU 0/BW200/gfx936 FP32 8 workloads 均 parity 通过，topology 占完整 helper `72%--75%`，geometry 占 `17%--19%`；未测新 HIP/Triton candidate、FP64 性能、API wall-clock 或端到端。 |
| LP-042 | `packages/ops/nvalchemiops/_hip_batch_query_materialize.py`；`_native/batch_query_materialize.cpp/.cu`；`probes/native_batch_cell_query_candidate_hip_boundary.py` | 对 native unordered Batch query candidate 以五字段稳定 rocPRIM radix sort、counts scan 和 public scatter 复现上游 `_sort_pairs` 的 matrix/count/shift 语义，形成 topology correctness candidate。 | 仅建立 HIP topology materialization 的隔离 correctness 边界；临时 workspace/字段 buffer 与 host-side pair-count sync 未优化，不改变 geometry/autograd、dispatcher、AOT、`auto` 或 `hip` capability。 | **不候选默认上游变更**：Hygon HIP sorting/scatter、workspace、后端选择和性能策略需本地维护；上游 `_sort_pairs` 继续作为 oracle。 | `reports/g2-batch-query-topology-materialization-hip-boundary.md`；CPU `21 passed`，HCU 0/BW200/gfx936 FP32/FP64、Batch mixed-PBC、full/half、empty、stream topology parity 通过；未测性能、FP64 timing、API wall-clock 或端到端。 |
| LP-043 | `probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-topology-materialization-benchmark.md` | 在固定 native candidate 上比较 Torch topology canonicalization/scatter 与 HIP rocPRIM sort/scan/scatter 的高 Batch 稳态 device time，记录 stream synchronization boundary 和 parity gate。 | 只提供 topology stage profile，不改变上游 query/public semantics、dispatcher、AOT、`auto` 或 `hip` capability；native 临时分配与 host pair-count sync 仍未优化，event time 不是 API wall-clock。 | **不候选默认上游变更**：Hygon HIP topology optimization、workspace reuse、后端选择和 benchmark 证据需本地维护；上游 ordering 仍由 Torch oracle 定义。 | `reports/g2-batch-query-topology-materialization-benchmark.md`；HCU 0/BW200/gfx936 FP32 8 workloads parity 通过，HIP/Torch median factor `0.830x--1.696x`，仅 `46×32` 局部收益；未测 FP64 performance、API wall-clock 或端到端。 |
| LP-044 | `probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-topology-materialization-profile.md` | 使用 DTK `hipprof` 对 HIP topology materialization candidate 的 prepare/order、五轮 sort、gather、scan 和 scatter 进行受限 kernel hotspot profile，覆盖 `46×32` 与 `92×64` 高 Batch 端点。 | 只提供 kernel 组成证据，不改变上游 query/public semantics、dispatcher、AOT、`auto` 或 `hip` capability；profile sample 不作为性能准入，exclusive scan、allocator 和 host sync 未从混合 trace 中独立量化。 | **不候选默认上游变更**：Hygon profile、single composite key、workspace 生命周期和后端选择需本地维护；上游 `_sort_pairs` 的五字段稳定顺序继续作为 oracle。 | `reports/g2-batch-query-topology-materialization-profile.md`；HCU 0/BW200/gfx936 FP32、四个 workload parity 通过；sort 相关 native kernel 每次约 `0.755/1.638 ms`（`46×32`/`92×64`）；未测 FP64 performance、API wall-clock 或端到端。 |
| LP-045 | `packages/ops/nvalchemiops/_hip_batch_query_materialize.py`；`_native/batch_query_materialize.cpp/.cu`；`probes/native_batch_cell_query_materialize_composite_hip_boundary.py`；`reports/g2-batch-query-topology-materialization-composite-key.md` | 验证保持上游 `_sort_pairs` public lexicographic order 的 single composite-key topology candidate：将 row/column/三维 signed PBC shift 编码到一个 signed `int64` key，单次 radix sort 后恢复 public topology。 | 只建立 composite-key 的字段布局、signed shift range、63-bit overflow 和 stream correctness 边界；int64 是 packed scalar storage，不是 gfx936 matrix-core capability。未改变 geometry/autograd、dispatcher、AOT、`auto` 或 `hip` capability，也未宣称性能收益。 | **不候选默认上游变更**：Hygon composite-key encoding、int64 radix sort、workspace 和后端选择需本地维护；上游 `_sort_pairs` 五字段稳定顺序继续作为 oracle。 | `reports/g2-batch-query-topology-materialization-composite-key.md`；CPU `5 passed`，HCU 0/BW200/gfx936 mixed-sign PBC fixture 默认/非默认 stream parity 与 overflow rejection 通过；未测性能、FP64 timing、API wall-clock 或端到端。 |
| LP-046 | `probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-topology-materialization-composite-key-benchmark.md` | 在相同 native unordered candidate 和 HIP-event contract 下，比较 Torch topology、五次 int32 stable-sort HIP candidate 与 single-int64-key HIP candidate，覆盖 `46/92 × 32/64`、uniform/clustered 高 Batch workload。 | 只提供 topology-stage device-time evidence，不改变上游 query/public semantics、dispatcher、AOT、`auto` 或 `hip` capability；两个 HIP candidate 的临时分配与 host pair-count sync 边界仍保留，event time 不是 API wall-clock。 | **不候选默认上游变更**：Hygon composite-key benchmark、int64 sort、workspace 生命周期和后端选择需本地维护；上游 `_sort_pairs` 五字段顺序继续作为 oracle。 | `reports/g2-batch-query-topology-materialization-composite-key-benchmark.md`；HCU 0/BW200/gfx936 FP32 8 workloads parity 通过，composite/five median factor `0.310x--0.406x`；未测 FP64 performance、API wall-clock、workspace reuse 或端到端。 |
| LP-047 | `probes/native_batch_cell_query_candidate_hip_boundary.py`；`reports/g2-batch-query-topology-materialization-composite-key-boundary.md` | 将 single composite-key topology candidate 并入已有 Batch query/materialization correctness boundary，与 Torch reference 和五次 HIP stable-sort candidate 并列验证。 | 只扩展 FP32/FP64、mixed-PBC、full/half、empty、stream 和 capacity 合同；既有 geometry/autograd 检查仍属于 Torch continuous path。未改变 query/public semantics、dispatcher、AOT、`auto` 或 `hip` capability。 | **不候选默认上游变更**：Hygon composite-key correctness boundary 和错误合同需本地维护；上游 `_sort_pairs` public ordering 继续作为 oracle。 | `reports/g2-batch-query-topology-materialization-composite-key-boundary.md`；HCU 0/BW200/gfx936 dtype/full-half/stream/empty/capacity parity 通过；未测性能、FP64 timing、API wall-clock 或端到端。 |
| LP-048 | `packages/ops/nvalchemiops/_hip_batch_query_materialize.py`；`_native/batch_query_materialize.cpp/.cu`；`probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-topology-materialization-workspace.md` | 为 single composite-key topology candidate 增加 caller-owned workspace size query、scratch ABI 和重复调用复用路径，分别测 HIP-event device time 与显式同步 API wall-clock。 | 只改变临时 workspace 生命周期，不改变排序字段、public order、query semantics、dispatcher、AOT、`auto` 或 `hip` capability；`candidate_counts.sum().item()` host sync 保留并单列后续实验。 | **不候选默认上游变更**：Hygon workspace ABI、生命周期和性能证据需本地维护；上游 `_sort_pairs` 语义继续作为 oracle。 | `reports/g2-batch-query-topology-materialization-workspace.md`；CPU `6 passed`，HCU 完整 boundary 与 8 个 benchmark workload parity 通过，reused/direct device factor `0.900x--1.026x`、API wall factor `0.902x--1.129x`；未测 host-sync removal、FP64 performance 或端到端。 |
| LP-049 | `packages/ops/nvalchemiops/_native/batch_query_materialize.cu`；`probes/native_batch_cell_query_candidate_hip_boundary.py`；`probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-topology-materialization-host-sync.md` | 移除五次 int32 与 single-int64 composite topology scatter 中的 `candidate_counts.sum().item()` host sync；按固定 candidate capacity 发射并在 device 侧跳过 padding，覆盖零 pair 边界及 device/API benchmark。 | 只改变 pair-count 到 scatter 的执行边界，不改变排序字段、public order、query semantics、geometry/autograd、dispatcher、AOT、`auto` 或 `hip` capability；固定容量 launch 的稀疏场景代价仍需后续评估。 | **不候选默认上游变更**：Hygon device-side scatter、launch policy 和后端选择需本地维护；上游 `_sort_pairs`/public materialization 语义继续作为 oracle。 | `reports/g2-batch-query-topology-materialization-host-sync.md`；CPU `6 passed`，HCU 0/BW200/gfx936 FP32/FP64/full/half/empty/stream/capacity/zero-count parity 通过；8 workload parity 通过，point28/point27 direct 描述性 device factor `0.817x--0.914x`、API factor `0.858x--1.042x`；未测 FP64 performance、geometry HIP/Triton、端到端或 runtime。 |
| LP-050 | `packages/ops/nvalchemiops/_torch_batch_query_materialization.py`；`probes/native_batch_cell_query_candidate_hip_boundary.py`；`probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-hybrid-torch-geometry.md` | 将 native HIP query + composite-key topology 与 Torch distance/vector geometry 组合为 hybrid query/materialization path，并比较完整 public outputs 与 Torch reference。 | 只增加 topology-to-geometry bridge 和窄 query/materialization benchmark；不接 dispatcher、AOT、`auto`、`hip` capability，不实现 native geometry/autograd，不覆盖 build 重复计时、rebuild、target/pair outputs 或 compile/opcheck。 | **不候选默认上游变更**：Hygon hybrid composition、Torch geometry bridge 和 HIP/Triton geometry 选择需本地维护；上游 public neighbor geometry 语义继续作为 oracle。 | `reports/g2-batch-query-hybrid-torch-geometry.md`；CPU `11 passed`，HCU 0/BW200/gfx936 FP32/FP64/full/half/mixed-PBC/empty/capacity/stream 完整 parity 通过；8 workload hybrid/Torch device factor `0.0120x--0.0257x`、API factor `0.0124x--0.0256x`；未测 native geometry、FP64 performance、build-inclusive end-to-end 或 runtime。 |
| LP-051 | `packages/ops/nvalchemiops/_hip_batch_query_geometry.py`；`packages/ops/nvalchemiops/_native/batch_query_geometry.cpp/.cu`；`probes/native_batch_cell_query_candidate_hip_boundary.py`；`probes/native_batch_cell_query_materialization_benchmark.py`；`reports/g2-batch-query-native-hip-geometry.md` | 在已 canonicalized 的 public topology 上增加 native HIP forward distance/vector geometry candidate，并与 Torch geometry 做 geometry-only 及 hybrid query/materialization 对照。 | 只建立 isolated forward HIP geometry ABI 和窄 benchmark；显式拒绝 autograd 输入，不实现 backward/二阶梯度，不接 dispatcher、AOT、`auto`、`hip` capability，不改变上游 query/public ordering，也不覆盖 build-inclusive、rebuild、target/pair outputs 或 compile/opcheck。 | **不候选默认上游变更**：Hygon HIP geometry kernel、后端选择和梯度接入策略需本地维护；上游 vector/distance 公式继续作为 reference。 | `reports/g2-batch-query-native-hip-geometry.md`；CPU `8 passed`，HCU 0/BW200/gfx936 FP32/FP64/mixed-PBC/full/half/empty/capacity/zero-count/stream parity 通过；8 workload geometry-only device/API factor `0.0784x--0.1220x`/`0.1092x--0.1537x`，窄 hybrid factor `0.0069x--0.0146x`/`0.0075x--0.0155x`；未测 native autograd、FP64 performance、build-inclusive end-to-end 或 runtime。 |
| LP-052 | `probes/native_batch_cell_build_query_materialization_benchmark.py`；`reports/g2-batch-cell-build-query-materialization-benchmark.md` | 在同一隔离 probe 中测量 cell-list build、native query candidate、composite topology materialization 和 geometry 的完整 Batch 邻居流程，并保留 Torch geometry 训练路径与 native HIP forward geometry 候选两条 native 组合。 | 仅新增 build-inclusive benchmark 和 parity gate，不改变上游 query/build 语义、dispatcher、AOT、`auto` 或 `hip` capability；固定 metadata/buffer/workspace、预热样本和 JIT 排除在计时外，不能把 isolated factor 扩大为默认 neighbor/MD 支持。 | **不候选默认上游变更**：Hygon native build/query/topology/geometry 组合、性能测量和后端选择需本地维护；上游 cell-list/query/geometry 逻辑继续作为 correctness reference。 | `reports/g2-batch-cell-build-query-materialization-benchmark.md`；HCU 0/BW200/gfx936 FP32，`46/92 × 32/64`、uniform/clustered 8 workload full parity 通过；native build-only/Torch `1.311x--1.399x`，native+Torch-geometry full device/API `0.0246x--0.0510x`/`0.0248x--0.0501x`，native-geometry full `0.0193x--0.0384x`/`0.0195x--0.0381x`；未测默认 runtime、rebuild/skin、FP64 performance、JIT/分配成本、autograd/compile、target/pair outputs 或 DomainParallel。 |
| LP-053 | `probes/native_batch_cell_build_breakdown_benchmark.py`；`reports/g2-batch-cell-build-breakdown-profile.md` | 拆分 native HIP Batch cell-list build 的 fused key/count、rocPRIM scan、atomic fill 和 full build，并以受限 hipprof 核验 wrapper/device-work 组成，为后续 fast path 选择单一优化假设。 | 仅增加 profiling/benchmark evidence，不改变上游 cell-list build 语义、native kernel、对外 validation、dispatcher、AOT、`auto` 或 `hip` capability；profile 的 module load 和单样本 trace 不作 steady-state 结论。 | **不候选默认上游变更**：Hygon performance diagnosis、trusted fast path 和后端策略需本地维护；上游/Torch cell-list ABI 继续作为 correctness reference。 | `reports/g2-batch-cell-build-breakdown-profile.md`；HCU 0/BW200/gfx936 FP32 8 workload CSR parity 通过，native/Torch full build `1.311x--1.423x`；74.5 万/149.1 万 cells 的 key/count/scan/fill 约 `0.80/0.70/0.81` 与 `0.80/0.98/1.04 ms`；未实现 direct fast path，未测其收益、runtime、FP64 performance、rebuild/skin、autograd/compile 或 DomainParallel。 |

| LP-054 | `packages/ops/nvalchemiops/_hip_batch_cell_build.py`；`probes/native_batch_cell_build_trusted_fastpath_benchmark.py`；`reports/g2-batch-cell-build-trusted-fastpath.md` | 在已由上层验证的精确 Batch buffer 集合上，绕过重复的 Python/custom-op ABI validation，直接串接既有 native HIP key/count、rocPRIM scan 和 atomic fill extension，形成 private trusted build fast path。 | 公开 `build_batch_cell_csr_hip_into` 的 validation/error contract、CSR/PBC/dtype 语义和 Torch reference 不变；trusted helper 不导出、不接 dispatcher、AOT、`auto` 或 `hip` capability，仅用于隔离性能证据。 | **不候选默认上游变更**：Hygon validation ownership、prevalidated plan/lifecycle 和 HIP 后端策略需本地维护；上游/Torch cell-list ABI 继续作为 correctness reference。 | `reports/g2-batch-cell-build-trusted-fastpath.md`；HCU 0/BW200/gfx936 FP32 8 workload direct/public/Torch CSR parity 通过，trusted/public HIP-event factor `0.0285x--0.0318x`，trusted/Torch `0.0398x--0.0423x`，CPU focused `26 passed`；未接 runtime，未测 FP64 performance、rebuild/skin、JIT/分配成本、autograd/compile 或 DomainParallel。 |
| LP-055 | `packages/ops/nvalchemiops/_hip_batch_cell_build.py`；`probes/native_batch_cell_build_trusted_fastpath_benchmark.py`；`reports/g2-batch-cell-build-trusted-plan.md` | 将 trusted build 从私有函数约定封装为 `_TrustedBatchCellBuildPlan`：初始化阶段执行一次 checked public build，固定 tensor/workspace/storage 生命周期内重复执行 direct native composition。 | 只增加显式 validation ownership 和 buffer lifetime contract；公开 build、Torch reference、CSR/PBC/dtype 语义不变，plan 不导出、不接 dispatcher、AOT、`auto` 或 `hip` capability。结构变化、capacity、workspace 或 storage 变化时必须重新初始化。 | **不候选默认上游变更**：Hygon prevalidated plan/lifecycle 和 runtime 策略需本地维护；上游/Torch cell-list ABI 继续作为 correctness reference。 | `reports/g2-batch-cell-build-trusted-plan.md`；HCU 0/BW200/gfx936 FP32 8 workload plan/public/Torch CSR parity 通过，trusted/public HIP-event factor `0.0281x--0.0313x`，trusted/Torch `0.0394x--0.0413x`，CPU focused `21 passed`；未接 runtime，未测完整 neighbor pipeline、FP64 performance、rebuild/skin、autograd/compile 或 DomainParallel。 |
| LP-056 | `probes/native_batch_cell_build_query_materialization_benchmark.py`；`reports/g2-batch-cell-build-query-materialization-trusted-plan.md` | 将 prevalidated build plan 接入完整 `build -> native query -> composite topology -> Torch geometry` 隔离 pipeline，并独立比较 public native、trusted plan 和 Torch reference。 | 只增加完整 forward benchmark/parity evidence，不改变上游 cell-list/query/topology/geometry 语义，不接 dispatcher、AOT、`auto` 或 `hip` capability；Torch geometry 继续作为训练/梯度路径，native geometry 旁路保持 forward-only。 | **不候选默认上游变更**：Hygon full-pipeline benchmark、runtime capability/错误合同和后端选择需本地维护；上游 cell-list/query/geometry 继续作为 correctness reference。 | `reports/g2-batch-cell-build-query-materialization-trusted-plan.md`；HCU 0/BW200/gfx936 FP32 8 workload 独立 output parity 通过，public native/Torch full factor `0.0249x--0.0501x`，trusted plan/Torch full `0.0121x--0.0257x`，trusted/public native `0.472x--0.544x`；未接 runtime，未测变胞/rebuild/skin、FP64 performance、half-list、target/pair outputs、compile/opcheck 或 DomainParallel。 |
| LP-057 | `packages/framework/nvalchemi/_backend.py`、`neighbors.py`、`hooks/neighbor_list.py`；`reports/g2-fixed-cell-neighbor-selection-contract.md` | 新增 `resolve_neighbor_list_backend`，将 `compute_neighbors` 与 `NeighborListHook` 的 PBC/no-PBC、full/half、MATRIX/COO、method strategy 和中央 `BackendSelection` 构造统一。 | 避免未来 native HIP runtime wrapper 在两个 framework 入口复制 capability/error 判断；只改变 framework selection construction，不改变 Torch reference、Warp、`auto` 或 registry capability 宽度。 | **不候选默认上游变更**：这是 Hygon runtime contract/wiring 边界；上游 neighbor topology 语义继续作为 reference。 | `reports/g2-fixed-cell-neighbor-selection-contract.md`；framework focused suite `34 passed, 1 warning`，`py_compile` 与 `git diff --check` 通过；未新增 HCU kernel 或性能证据。 |
| LP-058 | `packages/ops/nvalchemiops/_hip_batch_neighbor_runtime.py`；`packages/ops/test/torch/test_hip_batch_neighbor_runtime.py`；`reports/g2-native-hip-neighbor-runtime-abi.md` | 为 native HIP Batch neighbor isolated stages 冻结最小 runtime ABI/admission gate：checked/trusted build workspace、unordered query、composite-key topology 和 Torch geometry。 | 只定义本地 wrapper 的 workspace/lifetime、fixed-cell/full-list/Batch/dtype/gradient 和 unsupported-request error contract；不注册 `hip`、不改变上游 neighbor semantics、dispatcher、AOT 或 `auto`。 | **不候选默认上游变更**：Hygon native runtime admission、错误合同和阶段组合需本地维护；上游 cell-list/query/topology/geometry 继续作为 correctness reference。 | `reports/g2-native-hip-neighbor-runtime-abi.md`；CPU contract suite `15 passed`，`git diff --check` 通过；未新增 HCU execution、runtime registration 或性能证据。 |
| LP-059 | `packages/ops/nvalchemiops/_hip_batch_neighbor_runtime.py`；`packages/ops/nvalchemiops/_native/batch_query_materialize.cu`；`probes/native_batch_neighbor_runtime_hip_boundary.py`；`reports/g2-native-hip-neighbor-runtime-wrapper.md` | 增加显式 native HIP Batch neighbor wrapper，组合 trusted CSR build、native query、composite topology 和 Torch geometry；修复 composite workspace size-query 的 int32/int64 storage 边界。 | 只增加本地 ops wrapper、workspace size-query 修复、HCU parity/gradient evidence；不注册 `hip`、不改变上游 neighbor semantics、framework dispatcher、AOT、`auto` 或 Warp。 | **不候选默认上游变更**：Hygon runtime wrapper、workspace ownership 和后端接线需本地维护；上游 cell-list/query/topology/geometry 继续作为 correctness reference。 | `reports/g2-native-hip-neighbor-runtime-wrapper.md`；CPU ops `109 passed, 1 warning`；gfx936/BW200 HCU 0 的 `46×32`、`92×64` parity 和 `46×32` 一阶/二阶位置梯度通过；未测性能或 framework registry。 |
| LP-060 | `packages/ops/nvalchemiops/_hip_neighbor_executor.py`；`packages/ops/nvalchemiops/_backend_catalog/neighbors.py`；`packages/framework/nvalchemi/hooks/neighbor_list.py`；`probes/framework_neighbor_hip_boundary.py`；`reports/g2-framework-hip-neighbor-executor.md` | 将 Point37 native HIP Batch pipeline 接入公开 framework selection/ops generic executor ABI，登记固定 periodic full-list MATRIX capability，并保留明确 unsupported 边界。 | 只增加 Hygon 本地 registry/executor adapter、capacity/workspace orchestration 和 skin/rebuild rejection；不改变上游 neighbor semantics、Warp/Torch/auto 默认路径，也不宣称生产 performance。 | **不候选默认上游变更**：framework backend selection、HIP workspace 生命周期和后端策略需本地维护；上游 cell-list/query/topology 继续作为 correctness reference。 | `reports/g2-framework-hip-neighbor-executor.md`；ops `38 passed`，framework `11 passed, 1 warning`；gfx936/BW200 `HIP_VISIBLE_DEVICES=4` 的 `46×32`、`92×64` framework parity 通过；未测性能、MACE/FIRE2、native geometry backward 或 compile/opcheck。 |
| LP-061 | `packages/ops/nvalchemiops/torch_reference_cell_list.py`；`packages/ops/nvalchemiops/_hip_neighbor_executor.py`；`packages/ops/test/torch/test_torch_reference_cell_list.py`；`probes/framework_neighbor_hip_wallclock.py`；`reports/g2-framework-hip-neighbor-wallclock.md` | Point39 为 public framework HIP neighbor executor 建立 build-inclusive API wall-clock 与 Hook 边界证据；同时修复 Torch reference 大 Batch cell allocation/build 的 `max_nbins` 一致性，以及 HIP topology workspace allocation 前 `candidate_counts` 未初始化的问题。 | Torch reference build 与 allocator 使用同一 `max_nbins=8192`，HIP output buffer 在 query 前确定初始化；不改变上游 neighbor public ordering、Warp/auto 默认路径或 HIP capability 宽度。 | **不候选默认上游变更**：性能证据、HIP workspace 生命周期与 framework backend policy 属于 Hygon 本地实现；Torch reference 修复需作为独立上游候选审查。 | `reports/g2-framework-hip-neighbor-wallclock.md`；Torch cell-list CPU `14 passed`；gfx936/BW200 `HIP_VISIBLE_DEVICES=4` 的 46×32/92×64 public output parity 通过，warm HIP/Torch API median factor `0.09398x/0.07839x`；未覆盖完整 capability、MACE/FIRE2、compile/opcheck 或 `auto` 准入。 |

| LP-062 | `packages/ops/test/torch/test_hip_neighbor_executor.py`；`packages/framework/test/models/test_neighbors_torch_reference.py`；`probes/framework_neighbor_hip_contract.py`；`reports/g2-framework-hip-neighbor-contract.md` | Point40 framework HIP neighbor contract matrix：补充 FP32/FP64、空输入、容量溢出、unsupported request、重复 Hook 调用契约回归，并对照上游邻居接口约束。 | 上游 neighbor API 与 Torch reference 语义作为 oracle；native HIP 仅覆盖当前已验证的 periodic/full/matrix 路径。 | 仅完成窄 contract slice；未覆盖 `max_neighbors=None` 自动扩容、skin/rebuild、variable-cell、compile/opcheck 与 auto backend 选择。 | `reports/g2-framework-hip-neighbor-contract.md`；CPU ops `7 passed`、framework `11 passed, 1 warning`；gfx936/BW200 `HIP_VISIBLE_DEVICES=4` 的 46×32 FP32/FP64 parity、empty/capacity/rejection/repeated Hook 通过。 |
| LP-063 | `probes/framework_neighbor_hip_contract.py`；`reports/g2-framework-hip-neighbor-compatibility-gate.md`；`docs/STATUS.md`；`docs/PARALLEL_DEVELOPMENT_PLAN.md` | Point41 将显式 HIP framework neighbor contract probe 固化为 compatibility gate，并验证 `max_neighbors=None` 自动容量增长和有限上界。 | 仅收口当前 Hygon native HIP narrow capability；不改变上游 neighbor semantics、registry capability 宽度或 `auto`，不实现 lifecycle reuse。 | 阶段二在 periodic/fixed-cell/Batch/full-list/MATRIX/FP32/FP64/`skin=0` 范围内收口；HIP 自动容量采用本地 doubling policy，不承诺上游 `estimate_max_neighbors` 的最小 16/16 对齐 padded shape；完整上游 capability、MACE/FIRE2、skin/rebuild、变胞、native geometry backward 和 compile/opcheck 仍独立排期。 | `reports/g2-framework-hip-neighbor-compatibility-gate.md`；CPU ops `7 passed`、framework `11 passed, 1 warning`；gfx936/BW200 `HIP_VISIBLE_DEVICES=4` 的 46×32 gate 通过，容量 `1→8`、最大邻居 `6`、保守上界 `343`，active neighbor 集合与 Torch reference parity 通过。 |

本表中未带 `packages/` 前缀的 `test/...` 路径是相应 package 内的相对路径；新增跨包回归的当前实际目录为
`packages/framework/test/compatibility/`。历史条目的“工作树待提交”只描述当时快照，不表示当前 Git 状态。

清单维护规则：今后每次修改导入的上游文件，必须在同一提交或紧邻提交中新增一条
记录，包含文件、符号/范围、动机、默认路径影响、upstream candidate 标记和回归
测试指针。同步上游前先逐条重新审阅这些记录；不得直接在 `external/` 修改来绕过
清单。当前工作树尚未提交的代码、测试和报告仍以真实 Git diff 为准，不能把本表
当成已提交状态的替代品。
