# G0/G1 可重跑探针

先运行 `hy-smi` 确认资源并取得作业分配。以下 GPU 命令只在有设备访问权限的终端、已分配卡上运行；把 `<id>` / `<id0>,<id1>` 换为分配卡号，不默认使用全部 8 卡。G0 探针通常超时 60 秒，双卡 90 秒；P06 direct/wrapper 使用表中单独列出的 120/180 秒。沙箱可能隐藏 `/dev/dri` 和 `/dev/kfd`，不得把沙箱内不可见报告为服务器无卡。

| 探针 | 命令 | 本轮状态与范围 |
|---|---|---|
| P00 | `hy-smi --showproductname --showdriverversion --showmeminfo vram --showtopotype` | 已只读盘点，详见 ENVIRONMENT |
| 源码 | `.venv/bin/python probes/audit_upstream.py` | 退出 0，AST 与清单；不导入/执行上游 |
| P01/P04/segment CPU | `OMP_NUM_THREADS=1 .venv/bin/python probes/torch_probe.py --device cpu` | 结果见 reports/g0-validation.md |
| P01/P04/segment DCU | `HIP_VISIBLE_DEVICES=<id> OMP_NUM_THREADS=1 timeout 60 .venv/bin/python probes/torch_probe.py --device cuda` | 资源阻塞；张量、同步、FP64 一二阶、segment、FFT；不是 custom-op opcheck |
| P02 编译 | `hipcc --offload-arch=gfx936 probes/hip_probe.cpp -o artifacts/g0/hip_probe` | 编译退出 0；gfx936 为用户明确指定适配目标 |
| P02 运行 | `HIP_VISIBLE_DEVICES=<id> timeout 60 artifacts/g0/hip_probe` | 资源阻塞；仅 32 元素，不覆盖 gather/scatter/atomic |
| P03 | `source scripts/activate_hygon_env.sh project && HIP_VISIBLE_DEVICES=<id> TRITON_CACHE_DIR=$PWD/artifacts/triton-cache timeout 60 .venv/bin/python probes/triton_probe.py` | **2026-09-06 主机单卡通过**；BW200/UBB BW1000，1025 FP32 元素、tail mask、5 次预热/20 次调用，冷启动约 0.519 s，稳态约 24.6 μs/次；仅证明基础 Triton 能力，不等于生产 kernel 已注册 |
| P05-ref | `PYTHONPATH=packages/ops OMP_NUM_THREADS=1 .venv/bin/python probes/neighbor_lj_reference.py --device cpu`；HCU 版先 `source /opt/dtk-26.04/env.sh` 并设置 `PYTHONPATH=packages/ops HIP_VISIBLE_DEVICES=<id>` | Torch reference dispatcher：两体系、无 PBC、full/half neighbor matrix、LJ 能量/负梯度力，并记录实际 backend；CPU 与单卡 HCU 已通过，不代表生产 nvalchemiops API 已移植 |
| P05-framework | `source scripts/activate_hygon_env.sh project && pytest -q packages/framework/test/models/test_neighbors_torch_reference.py`；HCU MATRIX/PBC 版增加 `HIP_VISIBLE_DEVICES=<id>` | `compute_neighbors` 的显式 `torch_reference`/`auto` 入口、异构 batch、COO 转换和 PBC shifts；项目 `.venv` 的 reference/Hooks 回归已可直接运行；两包测试目录需分开启动，避免顶层 `test` 包名冲突 |
| P05-PBC | `source /opt/dtk-26.04/env.sh && PYTHONPATH=packages/framework:packages/ops HIP_VISIBLE_DEVICES=<id> timeout 60 /home/wangleping/codes/nvalchemi-toolkit/.venv/bin/python probes/pbc_neighbor_reference.py --device cuda` | full periodic neighbor topology、正交/三斜胞 shift；CPU 与单卡 HCU 已通过；不覆盖 half-list、skin/rebuild 或性能路径 |
| P05-PBC-LJ-NVE | `PYTHONPATH=packages/framework:packages/ops /home/wangleping/codes/nvalchemi-toolkit/.venv/bin/python probes/pbc_lj_nve_reference.py --device cpu`；HCU 版先加载 `/opt/dtk-26.04/env.sh` 并设置 `HIP_VISIBLE_DEVICES=<id>` | full-list 周期 LJ 的独立 FP64 对照与无 skin/no-switching 的短 velocity-Verlet 轨迹；CPU 与 BW200 HCU 已通过；独立探针不等同于完整 Warp-backed `nvalchemi.dynamics.NVE` |
| P05-skin | `source scripts/activate_hygon_env.sh project && python probes/skin_rebuild_batch_reference.py --device cpu`；HCU 版增加 `HIP_VISIBLE_DEVICES=<id> OMP_NUM_THREADS=1 timeout 90`；周期 NVE 另执行 `probes/pbc_lj_nve_reference.py --device <cpu|cuda> --skin 0.5` | Torch reference Hook 的两体系 Batch cached skin、只重建变化 system、全局索引偏移和周期 LJ cutoff 过滤；CPU 与 BW200/gfx936 HCU 已通过；不是生产 cell-list/容量扩展性能证据 |
| P05-capacity | `source scripts/activate_hygon_env.sh project && PYTHONPATH=packages/framework:packages/ops .venv/bin/python probes/neighbor_capacity_reference.py --device <cpu|cuda>`；HCU 版增加 `HIP_VISIBLE_DEVICES=<id> OMP_NUM_THREADS=1 timeout 120` | **2026-09-06 CPU/HCU 通过**；staging K grow/retry、idle shrink、override floor、异构 Batch 和 6 次 skin rebuild；不覆盖生产 cell-list、PBC 容量压力或规模化性能 |
| N2a-VV | `PYTHONPATH=packages/framework .venv/bin/python -m pytest -q packages/framework/test/compatibility/test_dynamics_reference_velocity_verlet.py`；设备版先 `source /opt/dtk-26.04/env.sh`，再设置 `HIP_VISIBLE_DEVICES=<id>` 运行 `probes/dynamics_reference_velocity_verlet.py --device cuda` | **2026-09-06 项目 `.venv` CPU/HCU 通过**；Warp-free 顶层 reference 的原位 velocity-Verlet、float32/64、异构 per-system `dt`、非法索引和无 Warp 导入；不等同于完整 `nvalchemi.dynamics.NVE` |
| N2a-kinetics | `PYTHONPATH=packages/framework .venv/bin/python -m pytest -q packages/framework/test/compatibility/test_dynamics_reference_velocity_verlet.py`；设备版先 `source /opt/dtk-26.04/env.sh`，再设置 `HIP_VISIBLE_DEVICES=<id>` 运行 `probes/dynamics_reference_kinetics.py --device cuda` | **2026-09-06 项目 `.venv` CPU/HCU 通过**；Warp-free 批量 kinetic energy/temperature、`[N]`/`[N,1]` masses、空批次和无 Warp 导入；不等同于公共 hooks 已接线 |
| P06-direct | `source scripts/activate_hygon_env.sh exploration` 后执行 `python probes/mace_probe.py --device cpu --checkpoint /home/wangleping/.cache/mace/MACE-OFF23_small.model`；HCU 版增加 `HIP_VISIBLE_DEVICES=<id> OMP_NUM_THREADS=1 timeout 120` | 探索环境的真实 MACE direct model；CPU 与 BW200/gfx936 HCU 已通过，26 个非零参数梯度；不证明 toolkit wrapper 或 dynamics |
| P06-wrapper | `source scripts/activate_hygon_env.sh exploration` 后执行 `python probes/mace_wrapper_reference.py --device cpu --checkpoint /home/wangleping/.cache/mace/MACE-OFF23_small.model [--cif <file1> <file2> ...]`；HCU 版增加 `HIP_VISIBLE_DEVICES=<id> OMP_NUM_THREADS=1 timeout 180` | 周期 reference 邻居已向量化；H₂O 单/双体系、单个 perf_46 和两个 `/data/csp_data/perf_46` CIF 的 wrapper CPU/HCU 均通过，`batch_ptr`/逐体系 energy/无跨体系边/逐体系总力均检查；向量化前的 180 秒超时保留在报告中作为优化前证据 |
| P07 | `source scripts/activate_hygon_env.sh project && HIP_VISIBLE_DEVICES=<id0>,<id1> OMP_NUM_THREADS=1 timeout 90 .venv/bin/torchrun --standalone --nproc-per-node=2 probes/distributed_probe.py` | **2026-09-06 主机双卡通过**；RCCL/NCCL backend 的 all-reduce 和双向 P2P 均通过，非域分解/LJ ownership 验收 |

MACE 审计和 checkpoint 证据见 `reports/g1-mace-dependency-audit.md`、`reports/g1-mace-wrapper-batch.md`。探索环境和项目 `.venv` 均已完成 MACE 最小依赖安装；项目环境用北外镜像解析到 `matscipy==1.1.1`，未替换海光 Torch/Triton。项目 `.venv` 的 pytest 基线和精确冻结见 `reports/g1-project-reference-pytest.md`、`configs/hygon-reference-lock.txt`。PhysicsNeMo 未安装，单进程 wrapper 通过可选导入路径验证，域并行/profiling 仍单独登记。探针输出 checkpoint/CIF SHA256、逐体系 batch 边界、energy/force 和 direct model 参数梯度；未知 checkpoint 不下载，不以 mock 代替真实 MLIP。

P04 自定义算子的 fake/meta、opcheck、gradcheck/gradgradcheck，P07 两卡 LJ ownership/pair 守恒尚待 G1 后端实现；P05-ref/P05-PBC/P05-PBC-LJ-NVE/P05-skin 和 P06-wrapper 仍是 Torch/reference 正确性链，不掩盖 half-list、完整 dynamics API、per-system rebuild 优化、训练 wrapper 混合二阶梯度、更大规模性能和生产 Triton/HIP/cell-list 缺口。P01/P02 本轮仍未运行；P03/P07 已完成基础能力探针，结果见 `reports/g0-capability-probes.md`。沙箱内 `/dev/kfd` 不可见，不能把沙箱失败当作设备能力失败；主机探针使用对应的 DTK shell 环境。无 NVIDIA 对拍 fixture。

运行时保存 stdout/stderr 和单独退出码至 artifacts/g0，并将脱敏数值摘要写 reports；记录代码提交、设备、dtype、种子。不得将未运行、编译成功或 CPU 成功写成 DCU verified。
